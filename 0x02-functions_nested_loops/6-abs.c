#include "main.h"

/**
 * _abs - This function computes the absolute value of an integer
 * @r: r is an ascii character
 * Return: always r (sucess) or -r (success)
 */
int _abs(int r)
{
if (r >= 0)
{
return (r);
}
else
{
return (-r);
}
}

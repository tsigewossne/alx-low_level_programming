#include "main.h"
#include <stdio.h>

/**
 * _isdigit- checks if the character is digit
 *@x :the number to be checked
 * Return: 1 for character that is digit or 0 for others
 */
int _isdigit(int x)
{
if (x >=48 && x<=57)
{
return (1);
}
return (0);
}
